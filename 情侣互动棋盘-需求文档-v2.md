# 情侣互动棋盘 (Couple Board Game) — 产品需求文档 v2

---

## 1. 项目概述

- **项目名称：** 情侣互动棋盘 (Couple Board Game)
- **运行环境：** 移动端网页（纯前端 SPA），必须能独立运行在手机浏览器中（Safari / Chrome / 微信内置浏览器等）。
- **技术栈：** 使用单个 `.html` 文件交付，内联 CSS 与 JavaScript，不依赖任何需要构建工具的框架。可使用 CDN 引入的轻量库（如 animate.css、canvas-confetti 等）辅助动画效果，但核心逻辑须为原生 JS。
- **核心玩法：** 两人（男方 & 女方）同屏轮流掷骰子，棋子在棋盘上前进，每次停下触发当前格子的专属互动事件。

---

## 2. 数据架构与同步机制

### 2.1 数据分层模型

本项目采用 **"远程主配置 + 本地覆盖"** 的两层数据架构：

```
┌─────────────────────────────────────────────┐
│  第一层：远程主配置（config.json）            │
│  ─ 作为所有客户端的统一数据源                 │
│  ─ 由管理者在后台统一维护                     │
│  ─ 所有打开网页的用户自动获取最新版本          │
├─────────────────────────────────────────────┤
│  第二层：本地覆盖（localStorage）             │
│  ─ 仅作用于当前浏览器 / 当前设备              │
│  ─ 用户通过页面 C 进行个性化增删改            │
│  ─ 优先级高于远程配置（覆盖合并）             │
└─────────────────────────────────────────────┘
```

**数据加载优先级（每次打开应用时）：**

1. 先请求远程 `config.json`，获取最新的主配置数据。
2. 检查 `localStorage` 中是否存在本地覆盖数据。
3. 若存在本地覆盖 → 使用本地数据（用户自己改过的版本）。
4. 若不存在本地覆盖 → 直接使用远程配置数据。
5. 若远程请求失败（离线/网络异常） → 回退使用代码内置的硬编码默认数据。

> **用户视角总结：** 管理者修改 `config.json` 后，所有**未做过本地修改**的用户下次打开即自动同步最新玩法；做过本地修改的用户不受影响，除非主动点击"恢复为云端版本"。

### 2.2 远程主配置文件（config.json）

`config.json` 与 `index.html` 部署在同一目录下，通过相对路径 `fetch('./config.json')` 加载。

**文件结构：**

```json
{
  "version": "2024-01-15",
  "maps": {
    "pure": {
      "name": "纯爱版",
      "emoji": "💕",
      "description": "从牵手开始的心动旅程",
      "totalSteps": 30,
      "themeColor": "#FFB6C1",
      "events": [
        { "id": 1, "event": "对视 10 秒不许笑" },
        { "id": 2, "event": "说出你第一次见到对方时的印象" }
      ]
    },
    "sweet": { ... },
    "deep": { ... },
    "sm": { ... }
  },
  "commonEvents": [
    { "id": "c1", "event": "真心话：说一个你从没告诉对方的小秘密" },
    { "id": "c2", "event": "休息一回合，给对方倒杯水" },
    { "id": "c3", "event": "大冒险：模仿对方一个招牌动作" }
  ]
}
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `version` | string | 配置版本号，用于判断远程是否有更新 |
| `maps` | object | 4 套方案的数据，key 为方案 ID |
| `maps.{id}.name` | string | 方案显示名称 |
| `maps.{id}.emoji` | string | 方案标识 emoji |
| `maps.{id}.description` | string | 方案简短描述 |
| `maps.{id}.totalSteps` | number | 该方案的棋盘总步数（各方案可不同） |
| `maps.{id}.themeColor` | string | 方案主题色 |
| `maps.{id}.events` | array | 该方案的专属玩法列表 |
| `commonEvents` | array | **通用玩法池**，用于补充不足的格子 |

### 2.3 动态棋盘规格与玩法补充机制

**核心规则：每套方案的棋盘格子数和专属玩法条数可以不同。**

| 方案 | 专属玩法条数（示例） | 棋盘总步数（示例） | 需补充条数 |
|------|---------------------|-------------------|-----------|
| 纯爱版 | 26 条 | 30 步 | 4 条（从通用池抽取） |
| 甜蜜版 | 64 条 | 70 步 | 6 条（从通用池抽取） |
| 深度版 | 29 条 | 40 步 | 11 条（从通用玩法和甜蜜版玩法中抽取） |
| SM版  | 25 条 | 40 步 | 15 条（从通用玩法、甜蜜版玩法和深度版玩法中抽取） |

> 以上数字仅为示例，实际以 `玩法.md` 和 `config.json` 中的数据为准。

**每局游戏开始时的玩法分配流程：**

```
1. 读取当前方案的 totalSteps（如 30）和 events 列表（如 26 条）
2. 计算差值 = totalSteps - events.length（如 30 - 26 = 4）
3. 若差值 > 0：
   → 从 commonEvents 通用池中随机抽取 4 条（不重复）
   → 将抽取的通用玩法与方案专属玩法合并
4. 将合并后的完整列表执行 Fisher-Yates 洗牌算法随机打乱
5. 打乱后的列表依次分配到格子 1 ~ totalSteps
6. 将分配结果保存到 game_state（确保刷新后顺序不变）
```

**注意：** 起点（第 0 步）无事件，终点（第 totalSteps 步）为最后一个事件格。

### 2.4 本地数据持久化（localStorage）

**存储 Key 设计：**

| Key | 内容 | 说明 |
|-----|------|------|
| `config_version` | 远程配置的 version 值 | 用于判断远程是否有更新 |
| `local_maps` | 用户本地修改后的完整地图数据 | 存在则覆盖远程数据 |
| `local_common_events` | 用户本地修改后的通用玩法池 | 存在则覆盖远程数据 |
| `game_state` | 当前游戏进度 | 含方案 ID、双方位置、当前回合、本局随机后的事件序列 |

**`game_state` 结构：**

```json
{
  "mapId": "pure",
  "totalSteps": 30,
  "malePosition": 5,
  "femalePosition": 3,
  "currentTurn": "male",
  "shuffledEvents": [
    { "step": 1, "event": "..." },
    { "step": 2, "event": "..." }
  ]
}
```

**进度恢复逻辑：** 若 `game_state` 存在且未完成，进入游戏页时弹窗询问"检测到上次未完成的游戏，是否继续？"，选择"继续"恢复，选择"新游戏"则清除旧进度重新开始。

---

## 3. 核心页面与流程设计

整个应用共 **3 个页面视图**（通过 JS 控制显示/隐藏切换，无需路由库）：

### 3.1 页面 A：主页（地图选择 & 入口）

**UI 元素：**

- **顶部：** 游戏标题"情侣互动棋盘"，搭配浪漫风格装饰。
- **中部：** 4 个地图方案的选择卡片，每个卡片显示：
  - 方案 emoji + 名称（如"💕 纯爱版"）
  - 简短描述/标语
  - 棋盘规格提示（如"共 30 步"）
  - 用不同主题色区分
- **底部：** "⚙️ 设置 / 编辑玩法" 按钮。

**交互逻辑：**

- 点击方案卡片 → 进入**页面 B（游戏页）**
- 点击设置按钮 → 进入**页面 C（设置页）**

---

### 3.2 页面 B：游戏进行页

**UI 布局（从上到下）：**

#### 顶部：状态栏
- 左上角："← 退出"按钮（点击后弹窗确认，确认则保存进度返回主页）
- 中间：**当前回合提示**（"♂ 男方的回合"或"♀ 女方的回合"，对应颜色高亮）
- 右上角：当前方案名称

#### 中部：棋盘视图
- **S 形蜿蜒路径**表示棋盘，格子从起点到终点排列。
  - 每个格子显示序号（1、2、3……N）
  - 起点标注"🚩 起点"，终点标注"🏆 终点"
  - 棋盘总格数**根据当前方案的 `totalSteps` 动态生成**
- **棋子：**
  - 男方：蓝色圆形（"♂"或"他"）
  - 女方：粉色圆形（"♀"或"她"）
  - 同格时并排显示，不重叠
- **棋盘可滚动**，棋子移动后自动滚动至当前位置居中

#### 底部：操作区
- 大而醒目的 **"🎲 掷骰子"** 按钮
- 下方显示双方当前步数（如"他：第 5 步 / 她：第 3 步"）

#### 弹窗层：事件展示

掷骰子后触发以下流程：
1. **骰子动画弹窗** — 骰子翻滚 1-2 秒，定格显示点数（1-6）
2. **棋子移动动画** — 逐格跳动前进到目标位置（每格 250-350ms，ease-in-out）
3. **互动任务弹窗** — 模态弹窗展示当前格子的任务文案，含：
   - 格子序号
   - 互动任务文案（大字，居中）
   - "✅ 完成 / 下一回合"按钮
4. 点击按钮后弹窗关闭，回合切换，顶部提示更新

**核心游戏逻辑：**

- 游戏开始时随机指定先手
- 双方初始在起点（第 0 步）
- 掷骰子 → 1-6 随机数 → 棋子前进
- **超出终点处理：** 当前位置 + 点数 > totalSteps → 直接停在终点
- **胜利流程：** 到达终点 → 全屏撒花动画 → 弹窗"🎉 [男方/女方] 获胜！奖励：可以要求对方做任意一件事！" → 按钮："🔄 再来一局" / "🏠 回到主页"

---

### 3.3 页面 C：设置与玩法编辑页

> **定位说明：** 页面 C 的所有修改仅作用于**当前浏览器 / 当前设备**（保存到 localStorage）。如需所有用户同步生效，需通过修改远程 `config.json` 实现（见第 4 节）。

**UI 布局：**

#### 顶部
- "← 返回主页"按钮 + 页面标题"编辑玩法"

#### 方案切换标签栏
- 5 个 Tab：纯爱版 / 甜蜜版 / 深度版 / SM版 / **通用玩法池**
- 点击切换显示对应内容

#### 玩法列表区域

以卡片列表形式展示当前 Tab 下的所有玩法，每条玩法卡片包含：
- 左侧：序号（自动编号，不可手动修改）
- 中间：文本输入区域（`<textarea>`），显示并可编辑该条玩法的文案内容
- 右侧：**🗑️ 删除按钮**（点击后弹窗确认"确定删除这条玩法吗？"，确认后删除该条）

#### 底部操作区

| 按钮 | 功能 |
|------|------|
| **➕ 新增玩法** | 在当前方案/通用池的列表末尾新增一条空白玩法，光标自动聚焦到新输入框 |
| **💾 保存修改** | 将当前所有修改写入 localStorage，Toast 提示"保存成功 ✅" |
| **☁️ 恢复为云端版本** | 弹窗确认后，清除当前方案的本地覆盖数据，重新从远程 config.json 加载，恢复为管理者设定的统一版本 |
| **🔄 恢复为内置默认** | 弹窗确认后，清除本地覆盖，同时忽略远程配置，恢复为代码内置的硬编码默认数据（作为最终兜底） |

#### 棋盘步数设置

在每个方案 Tab 的列表顶部，新增一个输入项：
- **"棋盘总步数"** — 数字输入框，显示当前方案的 `totalSteps`，可手动修改。
- 修改后，若 totalSteps > 玩法条数，游戏时会自动从通用池补充；若 totalSteps < 玩法条数，游戏时会随机抽取 totalSteps 条使用。
- 修改后随保存按钮一起生效。

---

## 4. 后台配置管理方案

### 4.1 工作方式

`config.json` 是面向管理者（你）的后台统一配置文件，部署在与 `index.html` 相同的服务器目录中。

**管理者更新流程：**

```
1. 在本地编辑 config.json（增删改任意方案的玩法、调整步数等）
2. 更新 version 字段（如改为当天日期 "2024-06-15"）
3. 将修改后的 config.json 上传 / 部署到服务器
4. 所有用户下次打开网页时自动拉取最新配置
```

**同步到所有用户的机制：**

```
用户打开网页
    ↓
fetch('./config.json') 获取远程配置
    ↓
比较远程 version 与本地 config_version
    ↓
┌─ 版本不一致（有更新）：
│   ├─ 用户无本地修改 → 自动静默更新为最新数据
│   └─ 用户有本地修改 → 弹窗提示：
│       "检测到管理员已更新玩法配置，是否同步最新版本？
│        （同步将覆盖你的本地修改）"
│       [同步更新] [保留我的版本]
│
└─ 版本一致（无更新）：
    → 继续使用当前数据，无提示
```

### 4.2 config.json 维护指南

管理者编辑 `config.json` 时的注意事项：

1. **新增玩法：** 在对应方案的 `events` 数组末尾新增条目，id 递增。
2. **删除玩法：** 删除对应条目即可，id 不需要连续。
3. **修改玩法：** 直接修改 `event` 字段的文案。
4. **调整步数：** 修改 `totalSteps` 值。
5. **修改通用池：** 编辑 `commonEvents` 数组，增删改均可。
6. **每次修改后务必更新 `version`字段**，否则客户端不会触发更新检测。

### 4.3 部署方式建议

由于本项目为纯前端静态文件，部署方式可选：

| 方式 | 操作 | 适合场景 |
|------|------|---------|
| **GitHub Pages** | 将 `index.html` + `config.json` 推送到 GitHub 仓库，开启 Pages | 免费、版本管理方便 |
| **静态文件服务器** | 上传到任意支持静态托管的服务器（Nginx、Vercel、Netlify 等） | 自有服务器 |
| **本地局域网** | 用 `python -m http.server` 或类似工具启动本地服务 | 仅限同一局域网内使用 |

> ⚠️ 注意：`config.json` 通过 `fetch()` 加载，因此**不能直接双击打开 `index.html`**（`file://` 协议下 fetch 会被浏览器安全策略拦截）。必须通过 HTTP 服务器访问。代码中应在 fetch 失败时自动回退到内置默认数据，保证离线/本地场景也能正常游玩。

---

## 5. UI / 视觉设计规范

### 5.1 整体风格

- **设计基调：** 浪漫、暧昧、精致，兼顾趣味性和情趣感。避免过于卡通低幼。
- **配色方案：** 以深色或暖色为底（如渐变的深紫-玫瑰金、暗夜蓝-霓虹粉），搭配高亮强调色。各方案微调主题色：

| 方案 | 主题色参考 |
|------|----------|
| 纯爱版 | 樱花粉 #FFB6C1 + 白 |
| 甜蜜版 | 珊瑚橙 #FF7F50 + 暖金 |
| 深度版 | 酒红 #8B0000 + 深紫 |
| SM版  | 黑 #1A1A2E + 暗红 #C62828 |

- **字体栈：** `-apple-system, "PingFang SC", "Noto Sans SC", "Microsoft YaHei", sans-serif`
- **圆角与阴影：** `border-radius: 12px-20px`，柔和 `box-shadow`

### 5.2 动画与交互

| 动画 | 实现方式 | 时长 |
|------|---------|------|
| 页面切换 | 淡入淡出 / 滑入滑出 | 300-400ms |
| 按钮按压 | `transform: scale(0.95)` + transition | 150ms |
| 骰子翻滚 | CSS animation 旋转 | 1-2s |
| 棋子移动 | 逐格跳动 + ease-in-out | 每格 250-350ms |
| 弹窗出现 | 底部滑入 / 缩放弹出 + 半透明遮罩 | 300ms |
| 胜利庆祝 | canvas-confetti CDN 撒花 | 3-5s |

### 5.3 移动端适配

- **最小适配：** 375×667（iPhone SE），向上兼容至 430×932（iPhone Pro Max）
- **字体：** 正文 ≥ 14px，按钮 ≥ 16px，弹窗任务文案 ≥ 18px
- **触摸目标：** 可点击元素最小 44×44px
- **安全区域：** 底部操作区使用 `env(safe-area-inset-bottom)`
- **禁止缩放：** viewport 设置 `user-scalable=no`

---

## 6. 边界情况与异常处理

| 场景 | 处理方式 |
|------|---------|
| 骰子点数导致超出终点 | 棋子直接停在终点格，触发胜利流程 |
| 两个棋子在同一格 | 并排显示，不重叠 |
| 刷新页面 | 通过 localStorage 中的 game_state 恢复进度 |
| 游戏中途退出 | 弹窗确认，确认后保存进度返回主页 |
| 远程 config.json 加载失败 | 回退到代码内置默认数据，控制台输出警告 |
| localStorage 不可用或被清空 | 回退到远程配置或内置默认数据 |
| 专属玩法条数 < totalSteps | 从通用池随机补充差额条数 |
| 专属玩法条数 ≥ totalSteps | 随机抽取 totalSteps 条使用 |
| 通用池不足以补充 | 取通用池全部 + 专属全部，totalSteps 以实际可用总数为准，Toast 提示 |
| 连续快速点击掷骰子 | 动画期间禁用按钮（disabled），防止重复触发 |
| 设置页删除玩法至 0 条 | 提示"至少保留 1 条玩法"，阻止删除最后一条 |
| 设置页保存空文案 | 高亮空输入框并提示"玩法内容不能为空" |

---

## 7. 交付清单

| 序号 | 文件 | 说明 |
|------|------|------|
| 1 | `index.html` | 完整可运行的单文件应用（HTML + CSS + JS），内含硬编码的默认玩法数据作为兜底 |
| 2 | `config.json` | 远程主配置文件，含 4 套方案数据 + 通用玩法池，部署在同一目录下 |
| 3 | `玩法.md` | 原始玩法数据（已提供，作为开发参考，内容需同步内置到 index.html 和 config.json 中） |

---

## 8. 补充说明

- 所有文案语言为**简体中文**。
- 代码注释使用中文，方便后续维护。
- 使用语义化 HTML 标签和 `aria-label` 等无障碍属性。
- 首屏加载应在 1 秒内完成渲染。
- `config.json` 的 fetch 请求应添加**缓存破坏参数**（如 `?t=${Date.now()}`），确保每次获取最新版本而非浏览器缓存。
